<configuration debug="false">

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSS'Z', UTC} | %-20.20thread | %5level | %-25.25logger{25} | %m%n</pattern>
    </encoder>
  </appender>

  <!-- individual package levels can be change -->
  <!--<logger name="io.netty" level="INFO"/>-->

  <!-- ConstructSnapshot too spammy for some of the testParallel cases -->
  <!-- See deephaven-core#636 -->
  <logger name="io.deephaven.db.v2.remote.ConstructSnapshot" level="WARN"/>

  <root level="info">
    <appender-ref ref="STDOUT" />
  </root>
</configuration>
